name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  go-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Run unit tests
        run: go test -race ./...

  kiox-provider-smoke:
    runs-on: ubuntu-latest
    needs: go-test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Smoke test packaged kiox provider
        uses: sourceplane/kiox-action@v2
        with:
          run: |
            bash ./scripts/smoke-kiox-provider.sh
          artifacts: |
            .kiox-artifacts/status.txt
            .kiox-artifacts/view.txt
            .kiox-artifacts/run.txt
            .kiox-artifacts/runs/demo-workflow/kiox-smoke-test/state.json
            .kiox-artifacts/runs/demo-workflow/kiox-smoke-test/context.json
            .kiox-artifacts/runs/demo-workflow/kiox-smoke-test/steps/Build_Message.json
            .kiox-artifacts/runs/demo-workflow/kiox-smoke-test/steps/Send_Message.json
          artifact-name: kiox-provider-smoke